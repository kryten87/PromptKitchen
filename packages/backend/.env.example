# .env.example for Prompt Kitchen Backend

# Path to the SQLite database file
# To use SQLite, set this variable to the path of your database file.
# DB_FILE=./dev.sqlite3

# ---
# Or, for PostgreSQL connection:
# To use PostgreSQL, set these variables.
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=


# Add other environment variables below as needed
# Example:
# API_KEY=your-api-key-here

# Google OAuth 2.0 credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_OAUTH_REDIRECT_URL=http://localhost:3000/auth/google/callback

# Session secret for signing cookies (change this to a strong random value in production)
SESSION_SECRET=your-session-secret

# Frontend URL for backend-to-frontend redirect after OAuth
# FRONTEND_URL is the base URL of your frontend app (used for OAuth redirect after login)
FRONTEND_URL=http://localhost:5173

# ---
# OpenAI API configuration for LLM integration
# The API key for authenticating with the OpenAI API. Required for prompt execution and test suite runs.
OPENAI_API_KEY=your-openai-api-key-here
# (Optional) The base URL for the OpenAI API. Override this if using a proxy or Azure OpenAI endpoint.
# Default is https://api.openai.com/v1
OPENAI_API_BASE_URL=https://api.openai.com/v1

# ---
# Instructions:
# 1. Copy this file to .env and fill in actual values as needed.
# 2. Do NOT commit your real .env file with secrets to version control.

# ---
# Enhanced Test Matching Config Defaults
# Maximum bytes allowed for a single assertion JSON
PK_MAX_ASSERTION_JSON_BYTES=65536
# Maximum bytes allowed for test result details
PK_MAX_TEST_RESULT_DETAILS_BYTES=524288
# Maximum length of regex source string
PK_REGEX_MAX_SOURCE_LEN=1024
# Maximum length of test string for regex
PK_REGEX_MAX_TEST_STR_LEN=100000
# Allowed regex flags (i, m, s, u)
PK_REGEX_ALLOWED_FLAGS=imsu

# ---
# Database Keep-Alive
# Set to 'true' to enable periodic database queries to prevent database deactivation
# The service will run a simple SELECT query every 24 hours to keep the database active
KEEP_ALIVE_ENABLED=false
