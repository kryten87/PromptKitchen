{
  "name": "@prompt-kitchen/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && rm -rf tsconfig.tsbuildinfo && tsc -p tsconfig.json && rm -rf dist/migrations/*.d.ts* && rm -rf dist/migrations/*.js.map",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5"
  },
  "dependencies": {
    "fast-deep-equal": "^3.1.3",
    "jsonpath-plus": "^10.3.0",
    "yup": "^1.6.1"
  }
}
